# gitIssuesCount
This program can be used to view the issue stats from any public repository in git.
It is a front end application developed using Angular. No backend is necessarily required to reach the target as public Github APIs can be fired from anywhere, any browser. Getting the data and manipulation of data can be from the front end itself.

Node.js is used to deploy the anguar code to backend.

The URL to view the result is : 
